namespace:
  name: app-prod

externalSecrets:
  enabled: true
  remoteKeys:
    host: secret/data/gitops-galaxy/prod/database
    port: secret/data/gitops-galaxy/prod/database
    database: secret/data/gitops-galaxy/prod/database
    username: secret/data/gitops-galaxy/prod/database
    password: secret/data/gitops-galaxy/prod/database
    jwt: secret/data/gitops-galaxy/prod/database

database:
  secretName: app-db-credentials
  keys:
    host: host
    port: port
    database: database
    username: username
    password: password

frontend:
  image:
    repository: docker.io/rasmusjoenurm/frontend
    tag: "1.0.26"
  replicaCount: 3
  backendBaseUrl: http://myapp-backend:8080

backend:
  image:
    repository: docker.io/rasmusjoenurm/backend
    tag: "1.0.26"
  replicaCount: 3

ingress:
  enabled: false
