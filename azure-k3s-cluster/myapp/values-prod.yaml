namespace:
  name: app-prod

externalSecrets:
  enabled: false

database:
  secretName: app-db-credentials
  keys:
    host: host
    port: port
    database: database
    username: username
    password: password
  host: "cluster-maximum-snail-pg-prod.postgres.database.azure.com"
  port: "5432"
  name: "gitops"
  username: "pgadmin"
  password: "a7lvFzKl@LRa%T^5FicW!-Cs"

appSecrets:
  keys:
    jwt: jwt_key
  jwt_key: "prod-2b8d4f1a9e6c3b7d0f5a8e2c1b4d9f6a"

frontend:
  image:
    repository: docker.io/rasmusjoenurm/frontend
    tag: "1.0.8"
  replicaCount: 3
  backendBaseUrl: http://myapp-backend:8080

backend:
  image:
    repository: docker.io/rasmusjoenurm/backend
    tag: "1.0.8"
  replicaCount: 3

ingress:
  enabled: false
